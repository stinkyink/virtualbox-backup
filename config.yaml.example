offsite-backend: glacier  # or s3
virtual-machines:
  pause-for-snapshot: true
  include:
    - VM Name or UUID
    - VM Name or UUID
  offsite-exclude: []
local:
  archive-list-file: './archives.csv'
  out-dir: './Out'
  commands:
    vbox-manage: 'VBoxManage'
    lvm: 'sudo lvm'
    mount: 'sudo mount'
    umount: 'sudo umount'
encryption:
  key: XXXX
aws:
  access-key: XXXX
  secret-key: XXXX
  region: eu-west-1
  upload-chunk-size: 67108864 #64MiB
  glacier:
    vault: Backups
  s3:
    bucket: Backups
